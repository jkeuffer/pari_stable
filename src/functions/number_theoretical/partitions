Function: partitions
Section: number_theoretical
C-Name: partitions
Prototype: LDGDG
Help: partitions(k,{a=k},{n=k})): return vector of partitions of k, a positive
 integer. Restriction can be put on the length of the partitions with parameter
 n (n=nmax or n=[nmin,nmax]) and on the range of X[i] with parameter a (a=amax
 or a=[amin,amax]). By default remove zeros, but one can set amin=0 to get X of
 fixed length nmax (=k by default).
Doc: returns vector of partitions of the integer $k$ (negative values return
 \kbd{[]}, $k = 0$ returns the trivial partition of the empty set).

 Optional parameters $n$ and $a$ can be:

 \item $n=nmax$ (resp. $n=[nmin,nmax]$) to restrict partitions to have length
 less than $nmax$ (resp. length between $nmin$ and $nmax$), where the length is
 the number of non zero entries.

 \item $a=amax$ (resp. $a=[amin,amax]$) to restrict
 the components of $X$ to be to integers less than $amax$ (resp. between $amin$
 and $amax$).

 By default, remove zero entries on the left except if $amin\leq0$, in which
 case $nmin$ is ignored and $X$ is of constant length $nmax$.

 A partition is given by a \typ{VECSMALL}:
 \bprog
 ? partitions(4, 2)
 %1 = [Vecsmall([1, 1, 1, 1]), Vecsmall([1, 1, 2]), Vecsmall([2, 2])]
 @eprog\noindent
 correspond to $2+2$, $1+1+2$, $1+1+1+1$.
