Function: map
Section: programming/specific
C-Name: map0
Prototype: GG
Help: map(f, A): apply function f to each entry in A
Doc: Given a vector or list \kbd{A} and a \typ{CLOSURE} \kbd{f},
 returns the elements $f(x)$ where $x$ runs through \kbd{A}. If \kbd{A} is a
 matrix, return the matrix whose entries are the $f(\kbd{A[i,j]})$.
 \bprog
 ? map(x->x^2, [1,2,3,4])
 %1 = [1, 4, 9, 16]
 ? map(x->x^2, [1,2;3,4])
 %2 =
 [1 4]

 [9 16]
 @eprog
