There is a different directory for each value of "$asmarch". In each of them,
lies a file Makefile.SH that will be included in the general Makefile. It
contains rules for creating headers and object files in the build directory.

The list of
  headers files is given by "$khlist" (default: "pariinl")
  object  files is given by "$kernel" (default: "kernel mp mpinl")

*******************************************************************
The "level 0 kernel" symbols are :
  overflow hiremainder
  addll addllx subll subllx shiftl shiftlr mulll addmul
  divll bfffo (+ tabshi)
For a generic architecture (no assembler kernel), they are defined in
"none/level0.c" and, if inlining is possible (C++ or gcc), with
"none/level0.h". Otherwise, in $asmarch/level0.s or $asmarch/level0.h
respectively. The corresponding object file is "kernel.o".

*******************************************************************
The "level 1 kernel" symbols are many. Some of them can be inlined, the
others not.
The "inline level 1" symbols are :
  addsii addssmod adduumod addssz affii affsi affui affsr cgetg cgeti cgetr
  cmpir cmpsr divise divisii divisz divrrz divsiz divss divssmod divssz
  dvmdiiz dvmdis dvmdisz dvmdsi dvmdsiz dvmdss dvmdssz evallg evallgef
  evalvalp evalexpo expi gtodouble icopy icopy_av itor itos itou modis mpabs
  mpadd mpaff mpcmp mpcopy mpdiv mpdivis mpdivisis mpmul mpneg mpsub mulsii
  mulssmod mulssz new_chunk random_bits realun realzero realzero_bit resiiz
  resis ressi rtor shiftr smodis stor stoi subii subir subri subrr subsi subsr
  subssmod umodui umuluu utoi vali

The "not-inline level 1" symbols are :
  absi_cmp absi_equal absr_cmp addii addir addrr addsi addsr addss affir
  affrr bezout cbezout cgiv cmpii cmprr cmpsi dbltor diviiexact diviuexact
  diviiz divir divis divis_rem diviu_rem divri divrr divrs divsi divsi_rem
  divsr dvmdii egalii gcdii invmod ishiftr isqrti modii modiiz modiu modsi
  modss modui mpdivz mpent mptrunc mulii mulir mulrr mulsi mulsr mulss mului
  muluu pari_rand31 pari_rand randomi ratlift resmod2n resss rtodbl shifti
  shift_r shifti3 smodsi sqri truedvmdii umodiu vals
