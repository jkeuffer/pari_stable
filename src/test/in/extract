A=vector(100,i,i);
for(i=35,99, print1(vecextract(A,1<<i)))
for(i=20,25, print(vecextract(A,1<<100-2-1<<i-1<<(2*i)-1<<(3*i))))
vecextract(A,"2..4")
vecextract(A,"-2..-4")
vecextract(A,"^65")
vecextract(matid(3),"2..","..")
vecextract(matid(3),"-2..","-1..")
matsize(vecextract(matid(3), 0, 3))
matsize(vecextract(matid(3), [], 3))
matsize(vecextract(matid(6), 3, 0))

fun = [Vec,Vecrev,Col,Vecsmall];
obj = [1, 'x, Pol([5,4,3,2,1]), List([1,2,3]), List([1,2,3,4,5])];
{
  for(i=1, #fun,
    my (f = fun[i]);
    print(f, ":");
    for (j=1, #obj,
      my(p = obj[j]);
      print(f(p, 4));
      print(f(p,-4));
    )
  );
}
