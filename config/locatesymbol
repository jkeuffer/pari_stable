try=`(nm $lib | grep $sym)2>/dev/null`
if test -z "$try"; then
  if ((file $lib | grep "stripped" | grep -v "not stripped")2>/dev/null); then
  # stripped dynamic library
  try=`(strings $lib | grep $sym)2>/dev/null`
  fi
fi
